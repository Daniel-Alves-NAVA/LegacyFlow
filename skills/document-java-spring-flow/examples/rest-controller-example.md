# Exemplo: REST Controller

## Contexto

Fluxo iniciado por um endpoint REST exposto por um `@RestController`.

## Saída esperada

- entrypoint identificado pela anotação Spring MVC;
- chamadas internas listadas;
- endpoints incluídos na seção correspondente;
- dependências externas confirmadas por evidência de código ou configuração.
