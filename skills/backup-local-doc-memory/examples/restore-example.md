# Exemplo: Restauração de Memória

## Contexto

Deseja-se restaurar a memória local a partir de um backup anterior.

## Saída esperada

- validação do pacote de backup;
- restauração das pastas `memory`, `summaries` e `runs`;
- atualização do `manifest.json` quando apropriado.
